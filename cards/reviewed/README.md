# Reviewed Cards

本目录只保存人工审核通过的卡片。

当前没有预置 reviewed 卡片。示例内容先放在 `cards/drafts/`，需要人工确认后再通过：

```bash
npm run review:card -- <card-id-or-draft-json-path> --dry-run
npm run review:card -- <card-id-or-draft-json-path>
```

迁移到本目录。
